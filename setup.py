from setuptools import setup

setup(name='pipe 00 - subdomain enumeration',
      description='finding domains',
      packages=['pipeline'],
      author='aydin nemati',
      author_email='',
      install_requires=[
            'gaiasdk>=0.0.16',
      ])